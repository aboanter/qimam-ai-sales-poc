// Analyst V2.5 semantic layer — evidence & confidence.
const upstreamFetch=global.fetch;
const ANTHROPIC_URL='https://api.anthropic.com/v1/messages';
const PRESENTATION_SYSTEM='You output only the JSON object described in the instructions below — no other text.';
const ANALYST_SYSTEM_MARKER='You are the quantitative analyst for Qimam AI Sales';
function parseLoose(text){return JSON.parse(String(text||'').replace(/^```json\s*/i,'').replace(/^```\s*/,'').replace(/```\s*$/,'').trim())}
function parseJsonPrefix(text){const s=String(text||'').trimStart(),start=s.search(/[\[{]/);if(start<0)throw new Error('No JSON prefix found');const open=s[start],close=open==='{'?'}':']';let depth=0,inString=false,escaped=false;for(let i=start;i<s.length;i++){const ch=s[i];if(inString){if(escaped)escaped=false;else if(ch==='\\')escaped=true;else if(ch==='"')inString=false;continue}if(ch==='"'){inString=true;continue}if(ch===open)depth++;else if(ch===close){depth--;if(depth===0)return JSON.parse(s.slice(start,i+1))}}throw new Error('Unterminated JSON prefix')}
function extractPresentationEnvelope(body){if(!body||body.system!==PRESENTATION_SYSTEM||!Array.isArray(body.messages))return null;const msg=body.messages[body.messages.length-1];if(!msg||typeof msg.content!=='string')return null;const text=msg.content,q='Original question:\n',p='\n\nExecuted Odoo MCP plan:\n',r='\n\nLIVE factual Odoo results:\n',qi=text.indexOf(q),pi=text.indexOf(p),ri=text.indexOf(r);if(qi<0||pi<0||ri<0||!(qi<pi&&pi<ri))return null;try{return{question:text.slice(qi+q.length,pi).trim(),plan:JSON.parse(text.slice(pi+p.length,ri).trim()),results:parseJsonPrefix(text.slice(ri+r.length))}}catch(e){console.error('analyst-preload envelope parse error:',e.message);return null}}
function normalizeEvidence(x){if(!Array.isArray(x))return[];return x.slice(0,12).map(v=>typeof v==='string'?v:(v&&typeof v==='object'?v:null)).filter(Boolean)}
function normalizeStatement(v,defaultType='derived'){if(typeof v==='string')return{text:v,type:defaultType,confidence:defaultType==='hypothesis'?'low':'high',evidence:[]};if(!v||typeof v!=='object')return null;const type=['fact','derived','hypothesis'].includes(v.type)?v.type:defaultType;let confidence=['high','medium','low'].includes(v.confidence)?v.confidence:(type==='hypothesis'?'low':'high');if(type==='hypothesis'&&confidence==='high')confidence='medium';return{...v,type,confidence,evidence:normalizeEvidence(v.evidence)}}
function normalizeStatements(arr,defaultType){return Array.isArray(arr)?arr.slice(0,20).map(x=>normalizeStatement(x,defaultType)).filter(Boolean):[]}
function safeAnalysis(a){if(!a||typeof a!=='object')return null;return{version:'2.5',answerability:['complete','partial','insufficient'].includes(a.answerability)?a.answerability:'partial',scope:a.scope&&typeof a.scope==='object'?a.scope:{},facts:normalizeStatements(a.facts,'fact').slice(0,40),derivedMetrics:Array.isArray(a.derivedMetrics)?a.derivedMetrics.slice(0,30).map(x=>x&&typeof x==='object'?{...x,type:'derived',confidence:['high','medium','low'].includes(x.confidence)?x.confidence:'high',evidence:normalizeEvidence(x.evidence)}:x):[],rankings:Array.isArray(a.rankings)?a.rankings.slice(0,12):[],comparisons:Array.isArray(a.comparisons)?a.comparisons.slice(0,12):[],trends:normalizeStatements(a.trends,'derived').slice(0,12),anomalies:normalizeStatements(a.anomalies,'derived').slice(0,12),insights:normalizeStatements(a.insights,'derived').slice(0,12),caveats:normalizeStatements(a.caveats,'fact').slice(0,12)}}
async function runAnalyst(envelope,originalBody){const apiKey=process.env.ANTHROPIC_API_KEY||'';if(!apiKey)return null;const model=process.env.ANTHROPIC_MODEL||originalBody.model||'claude-sonnet-4-6';const system=`${ANALYST_SYSTEM_MARKER}. You receive the user's question, executed read-only Odoo plan, and exact live MCP results. Analysis only; do not design UI or request more data. Return ONLY JSON with keys: answerability, scope, facts, derivedMetrics, rankings, comparisons, trends, anomalies, insights, caveats.\n\nEVIDENCE & CONFIDENCE V2.5:\n- Every factual/analytical statement in facts, trends, anomalies, insights and caveats MUST be an object with {text,type,confidence,evidence}.\n- type must be fact|derived|hypothesis. fact = directly present in MCP evidence. derived = deterministic arithmetic/logical restatement directly supported by evidence. hypothesis = plausible interpretation not proven by retrieved data.\n- confidence must be high|medium|low. Facts directly returned by MCP are normally high. Deterministic arithmetic from complete compatible evidence is normally high. Hypotheses MUST NEVER be high; use medium or low.\n- evidence is an array referencing supporting operation names and, when useful, the exact fields/numbers used. Never cite evidence that is absent.\n- Never turn a hypothesis into a fact through wording. Use explicit uncertainty language such as قد/يحتمل/يُرجّح and state what should be checked.\n- Example: 26 orders totaling 26 SAR is a fact if evidence says so. 'These are test orders' is only a hypothesis unless an explicit test flag/name/evidence proves it.\n- Recommendations must not imply an unproven cause.\n\nCALCULATION RULES:\n- Use ONLY supplied MCP evidence and deterministic arithmetic. Preserve exact time/company/state/filter scope.\n- derivedMetrics objects should include {name,value,unit,formula,inputs,status,type,confidence,evidence}; status valid|not_applicable|insufficient.\n- Percentage change=(current-base)/base*100 only if base!=0. If base=0: status=not_applicable,value=null; never call 0->positive 100% growth.\n- Keep absolute difference separate from percentage change. Shares require compatible denominator. Averages require compatible nonzero denominator.\n- contribution_to_change=current_entity-previous_entity; rank by signed difference when requested.\n- rankings state metric,direction,scope,items in exact rank order. Prefer items {rank,label,value,secondaryValue}. Sort by requested metric before returning.\n- comparisons preserve both periods and distinguish absoluteDifference from percentageChange.\n- Trends describe supported patterns, not invented causes. Anomalies identify unusual evidence; explanations for why they occurred are hypotheses unless directly evidenced.\n- Missing evidence, zero-base ratios, incompatible scopes and ambiguity belong in caveats. Mark answerability partial/insufficient rather than guessing.\n- No markdown, HTML, CSS, chart choices or presentation instructions.`;const user=`Question:\n${envelope.question}\n\nExecuted plan:\n${JSON.stringify(envelope.plan)}\n\nLIVE MCP evidence:\n${JSON.stringify(envelope.results)}`;const resp=await upstreamFetch(ANTHROPIC_URL,{method:'POST',headers:{'content-type':'application/json','x-api-key':apiKey,'anthropic-version':'2023-06-01'},body:JSON.stringify({model,max_tokens:4000,system,messages:[{role:'user',content:user}]})});if(!resp.ok)return null;try{const payload=await resp.json(),text=(payload.content||[]).map(x=>x.text||'').join('').trim();return safeAnalysis(parseLoose(text))}catch(e){console.error('analyst-preload analysis parse error:',e.message);return null}}
global.fetch=async function analystFetch(url,options={}){let body=null,envelope=null;try{if(String(url).includes('api.anthropic.com/v1/messages')&&options.body){body=JSON.parse(options.body);if(!String(body.system||'').includes(ANALYST_SYSTEM_MARKER))envelope=extractPresentationEnvelope(body)}}catch{}if(envelope&&body){try{const analysis=await runAnalyst(envelope,body);if(analysis){const msg=body.messages[body.messages.length-1];msg.content+=`\n\nANALYST V2.5 — AUTHORITATIVE SEMANTIC ANALYSIS WITH EVIDENCE & CONFIDENCE:\n${JSON.stringify(analysis)}\n\nUse analyst derived metrics, comparisons and rankings as authoritative; MCP remains authoritative for raw facts. Preserve not_applicable/insufficient. Preserve evidence semantics: facts may be stated directly; derived conclusions may be stated when supported; hypotheses MUST be presented explicitly as possibilities, never as established facts, and should include a verification suggestion when useful. Do not upgrade confidence or certainty beyond the analyst.\n\nRANKING CONSISTENCY — HARD REQUIREMENT:\n- For the same ranking metric/scope, bar, pie, table, KPI list and narrative MUST use analyst ranking items in the exact same order. Build labels and values together from those items; never independently sort them.\n- Different order only for a clearly different metric/scope/direction. For descending top-N verify value[i]>=value[i+1] and reorder complete label/value rows together if needed.`;options={...options,body:JSON.stringify(body)}}}catch(e){console.error('analyst-preload non-fatal error:',e.message)}}return upstreamFetch(url,options)};
require('./presentation-validator-preload.js');
